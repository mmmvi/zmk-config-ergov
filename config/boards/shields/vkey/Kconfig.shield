# No whitespace after the comma or in your part name!
config SHIELD_VKEY_LEFT
    def_bool $(shields_list_contains,vkey_left)

# No whitespace after the comma or in your part name!
config SHIELD_VKEY_RIGHT
    def_bool $(shields_list_contains,vkey_right)